{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.py) if [ -x \"${CLAUDE_PROJECT_DIR:-.}/.venv/bin/ruff\" ]; then \"${CLAUDE_PROJECT_DIR:-.}/.venv/bin/ruff\" format \"$f\"; else ruff format \"$f\"; fi;; esac; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "ruff format"
          }
        ]
      }
    ]
  }
}
